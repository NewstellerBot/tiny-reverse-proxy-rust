# Project Guidelines

## Commits
- Never include `Co-Authored-By` lines in commit messages.
- Always split changes into separate logical commits (e.g., implementation, wiring, tests).

## Build
- Use `nix develop` (or `direnv allow`) to get the dev shell with all dependencies.

