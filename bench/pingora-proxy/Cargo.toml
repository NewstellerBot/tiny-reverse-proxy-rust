[package]
name = "pingora-bench-proxy"
version = "0.1.0"
edition = "2021"

[dependencies]
pingora = { version = "0.7", features = ["proxy"] }
async-trait = "0.1"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
