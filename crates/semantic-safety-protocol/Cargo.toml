[package]
name = "semantic-safety-protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = "0.13"
serde = { version = "1", features = ["derive"] }
tonic = { version = "0.12", features = ["transport"] }

[build-dependencies]
protoc-bin-vendored = "3"
tonic-build = "0.12"
